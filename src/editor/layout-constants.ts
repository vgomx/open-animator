/** Matches Tailwind `w-14` (3.5rem) at default root font size. */
export const ACTIVITY_RAIL_WIDTH = 56

/** Matches Tailwind `w-56` (14rem) at default root font size. */
export const LAYERS_PANEL_WIDTH = 224

/** Matches Tailwind `w-72` (18rem) at default root font size. */
export const PROPERTIES_PANEL_WIDTH = 288

export const MIN_PANEL_WIDTH = 180
export const MAX_PANEL_WIDTH = 480

/** Fixed label column width in the timeline. */
export const TIMELINE_LABEL_WIDTH = 168

/** Default horizontal scale: pixels per second of animation. */
export const TIMELINE_PX_PER_SECOND = 100

export const TIMELINE_ROW_HEIGHT = 32

export const RULER_X_SIZE = 24
export const RULER_Y_SIZE = 20

/** @deprecated Use RULER_X_SIZE for top ruler height. */
export const RULER_SIZE = RULER_X_SIZE
